import React from "react";
import { NotLoggedInDiv, NotLoggedInInfo } from "./style";

function NotLoggedInfo() {
  return (
    <NotLoggedInDiv>
      <NotLoggedInInfo>Not Logged In</NotLoggedInInfo>
    </NotLoggedInDiv>
  );
}

export default NotLoggedInfo;
